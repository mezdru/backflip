<script type="text/html" id="hit-template">
  {{{{raw}}}}
  <div class="record {{type}} pure-g">
    <div class="pure-u-1-3">
      <a href="{{url}}" class="picture">
        <img onload="resizeImg(this)" src="{{picture.url}}">
      </a>
    </div>
    <div class="pure-u-2-3">
      <a href="{{url}}" class="name">
        <h2>{{{_highlightResult.name.value}}}</h2>
      </a>
      <p>{{{_snippetResult.intro.value}}}</p>
    </div>
    <div class="pure-u-1 hashtags">
      {{#hashtags}}
      <a onclick="setSearch('{{tag}}')" class="cloud-element hashtag">
        <img src="{{picture.url}}">
        {{name}}
      </a>
      {{/hashtags}}
    </div>
    <div class="pure-u-1 contacts">
      {{#links}}
      <a href="{{url}}" class="{{class}} fa fa-{{icon}}" title="{{display}}" aria-hidden="true" target="_blank"></a>
      {{/links}}
    </div>
  </div>
  {{{{/raw}}}}
</script>
